# FastAPI and dependencies
# Using Pydantic 1.x to avoid Rust compilation issues with Python 3.14
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==1.10.13

# Database (PostgreSQL)
asyncpg==0.29.0
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9

# AI - OpenRouter (supports GPT-4, Claude, Llama, etc.)
openai==1.3.0  # OpenRouter uses OpenAI-compatible API
httpx==0.25.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.1
authlib==1.2.1
itsdangerous==2.1.2
pyotp==2.9.0  # For TOTP/MFA
user-agents==2.2.0  # For device detection

# CSV Processing & Data Analysis
# pandas==2.1.4  # Temporarily disabled due to Python 3.14 compatibility issues
chardet==5.2.0
sqlparse==0.4.4

# File Storage
boto3==1.29.7  # For S3-compatible storage
aiofiles==23.2.1

# Background Jobs & Caching
celery==5.3.4
redis==5.0.1

# WebSocket for Realtime
websockets==12.0

# Utilities
python-dotenv==1.0.0
email-validator==2.1.0
python-slugify==8.0.1

import { StubPage } from '../../_stub';
export const metadata = { title: 'Firebase to ZendBX — ZendBX Docs' };
export default function Page() {
  return <StubPage title="Firebase to ZendBX" parent="Migration Guides" parentHref="/docs/migrate/supabase" />;
}
